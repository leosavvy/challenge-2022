export enum Departments {
    IT = 'IT',
    Marketing = 'Marketing',
    CX = 'CX',
    Support = 'Support',
}

export const DEPARTMENTS_COUNT = Object.keys(Departments).length / 2;
export const MIN_SALARY = 1000;
export const MAX_SALARY = 5000;
